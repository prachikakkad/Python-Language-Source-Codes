a = 24
b = 6
c = 6

print(b is a)  # Returns false as both operands are different and we programmed is
print(b is not a)  # Returns true as both operands are different and we programmed is not
print(b is c)  # Returns true as both operands are same and we programmed is
print(b is not c)  # Returns false as both operands are same and we programmed is not