# Question :-
""" Can you change the values inside a list which is contained in set S ?
S = {8, 7, 12, “Harry”, [1, 2]}

Answer :- We can't add a list into a set and an set is always immutable."""
